# Development Knowledge Brain

이 저장소는 사용자의 개발 지식을 위한 외부 장기 기억이다.

## Core Rules

1. 자료 자체가 아니라 사용자가 실제로 이해한 내용을 저장한다.
2. `Current Understanding`에는 사용자가 직접 표현한 언어를 보존한다. LLM의 일반 설명으로 대체하지 않는다.
3. 사실 검증과 교정은 저장 전에 대화에서 수행한다.
4. 새 파일을 만들기 전에 기존 지식을 먼저 검색한다.
5. 입력 하나를 파일 하나로 취급하지 않는다. 필요하면 여러 지식 단위로 분해하거나 기존 문서에 병합한다.
6. 단순 키워드마다 파일을 만드는 식으로 과도하게 쪼개지 않는다. 지식 단위는 나중에 독립적으로 회상할 가치가 있는 생각 단위다.
7. `Connections`는 실제 존재하는 관련 지식 파일과의 의미 있는 관계다. 단순 태그나 키워드 목록으로 사용하지 않는다.
8. `Understanding Timeline`에는 사용자의 의미 있는 이해 변화만 기록한다. 파일 수정 이력은 Git에 맡긴다.
9. 저장량보다 회상, 복습, 현재 작업과의 연결을 우선한다.
10. 폴더와 메타데이터는 최소화한다. 지식관리 시스템 자체가 사용자의 부담이 되어서는 안 된다.
11. 세부 작업 절차는 `.agent/`의 workflow 문서를 따른다.
12. 이 저장소에서 생성하는 Git 커밋 메시지는 한국어로 작성한다.

## Priority

세부 workflow의 지침이 이 문서의 Core Rules와 충돌하면 항상 이 문서의 Core Rules를 우선한다.
